services:
  portfolio:
    image: ${ACR_REGISTRY}/${ACR_NAMESPACE}/cjyportfolio:$TAG
    container_name: portfolio-container
    ports:
      - "3001:3001"
    restart: always
    command: ["node", "server.js"]
    # AI 助手运行时环境变量（从服务器上的 .env 文件加载）
    env_file:
      - ./.env
